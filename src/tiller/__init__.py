"""Tiller: a rule-based Solana spot trading agent.

Package layout follows docs/SPEC.md. Nothing in this package performs network I/O at
import time; every external dependency is injected by ``tiller.cli``.
"""

__version__ = "0.1.0"
